<div style="width: 40%">
    <form method="post" action="{{ path('app_ressource_remove', {'id': ressource.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ressource.id) }}">
        <button class="btn btn-outline-danger" style="width: 100%">Delete</button>
    </form>
</div>
